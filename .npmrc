# Flat node_modules — indispensable pour Electron + modules natifs (better-sqlite3)
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
# Autoriser la compilation des modules natifs
enable-pre-post-scripts=true
